# jquery.color.js

jQuery plugin for font color setting

## usage

```javascript
// default(white)
$('#sample').color();

// font color assign
$('p').each(function() {
  $(this).color({"color":"#123456"});
});
```