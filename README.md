# jquery.color.js

jQuery plugin for font color setting

## usage

```javascript
// default(white)
$('#sample').agColor();

// font color assign
$('p').each(function() {
  $(this).agColor({"color":"#123456"});
});
```